server:
  port: 8083

spring:
  application:
    name: hello-config-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848

management:
  endpoints:
    web:
      exposure: include*